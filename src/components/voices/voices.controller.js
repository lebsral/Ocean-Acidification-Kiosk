'use strict';

angular.module('acidification')
  .controller('VoicesController', function ($sce, $timeout) {
            var controller = this;
            controller.state = null;
            controller.API = null;
            controller.currentVideo = 0;

  });
